---
  - hosts: applications
    become: yes
    tasks:
    - name: install webserver
      apt: 
        name: webserver
        state: present
      tags: mywebserver


    - name: install tree
      apt: 
        name: tree
        state: present
      tags: mytree


    - name: install git
      apt:
        name: git
        state: present
      tags: mygit 

