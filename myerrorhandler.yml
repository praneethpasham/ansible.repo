---
  - hosts: [webserver]
    become: yes
    tasks:
    - name: i want to execute linux commands
      command: hostname -f
      register: myresult


    - name: display above task result
      debug:
        msg: "{{myresult}}"

    