---
  - hosts: webservers
    become: yes
    tasks: 
    
    - name: install tree on ubuntu16
      apt:
        name: tree

    - name: install java
      apt:
        namee: java
      ignore_errors: yes

      
    - name: install git
      apt:
        name: git

    - name: install wget
      apt:
        name: wget
    
    

